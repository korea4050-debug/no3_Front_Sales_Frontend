/**
 * ============================================
 * Next.js 설정 파일 (next.config.ts)
 * ============================================
 *
 * Next.js 프로젝트의 빌드·실행 방식을 설정하는 파일입니다.
 * TypeScript로 작성되어 있어 타입 체크와 자동완성을 활용할 수 있습니다.
 *
 * 자주 쓰는 옵션 예시:
 * - basePath: '/my-app'  → 모든 경로 앞에 접두사 추가 (서브 경로 배포 시)
 * - images.domains: ['example.com']  → 외부 이미지 도메인 허용
 * - env: { API_URL: process.env.API_URL }  → 환경 변수를 클라이언트에 노출
 * - redirects() / rewrites()  → URL 리다이렉트·리라이트
 *
 * 현재는 기본값만 사용 중이며, 필요할 때 위 옵션들을 추가하면 됩니다.
 */

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
