/**
 * ============================================
 * 프로모션(Promotion) 페이지 — URL: /promotion
 * ============================================
 *
 * (dashboard) Route Group 안에 있으므로 URL은 /promotion 입니다.
 * 프로모션 목록, 기간 설정, 할인율 등 UI를 여기에 추가하면 됩니다.
 */

import React from "react";

const Page = () => {
  return (
    <div>
      Promotion Page
    </div>
  );
};

export default Page;
