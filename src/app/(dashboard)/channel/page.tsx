/**
 * ============================================
 * 채널(Channel) 페이지 — URL: /channel
 * ============================================
 *
 * (dashboard) Route Group 안에 있으므로 URL은 /channel 입니다.
 * 판매 채널(오프라인, 온라인, 제휴 등) 관련 화면을 이 페이지에 구현할 수 있습니다.
 */

import React from "react";

const Page = () => {
  return (
    <div>
      Channel Page
    </div>
  );
};

export default Page;
